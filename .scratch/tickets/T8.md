## Parent

- #1 — AI-Manager: Kitter 功能移植

## What to build

前端 Projects 页：项目选择器 + 有效技能视图。显示每个 agent 的有效技能（managed + unmanaged + built-in + plugin）、来源标记、context token 估算 + 警告/危险阈值。

## Acceptance criteria

- [ ] `ProjectsPage.tsx`：项目选择器（最近项目 + 浏览）、agent 网格、有效技能表
- [ ] 有效技能行：名称、来源（managed/unmanaged/built-in/plugin）、安装位置
- [ ] Context token 估算：每个 agent 的 token 预算 + 警告（>2000）/ 危险（>5000）标记
- [ ] 技能数量估算：每个 agent 的技能数 + 警告（>20）/ 危险（>50）标记
- [ ] 调用 `ListProjects`、`GetEffectiveSkills` API
- [ ] 空状态（无项目时显示引导）

## Blocked by

- #6 — T6: Go 后端项目 + 安装
