## Parent

- #1 — AI-Manager: Kitter 功能移植

## What to build

Go 后端数据模型 + JSON 持久化。实现 `internal/skill/`、`internal/tags/`、`internal/config/`（扩展）、`internal/platform/` 模块，包含 SkillLibrary、SkillRecord、SkillOrigin、InstallTarget、TagState、Tag 等类型，以及 `config.json`、`registry.json`、`tags.json` 的读写（原子写：tmp + rename）。持久化路径用 AI-Manager 自己的（macOS: `~/Library/Application Support/AIManager/` 等）。

## Acceptance criteria

- [ ] `internal/skill/` 包含 SkillSummary、SkillRecord、SkillOrigin（6 变体）、SkillSource、SkillGroup
- [ ] `internal/tags/` 包含 Tag、TagState（两层树：parent 不能有 children）、TagDocument（skills + projects）
- [ ] `internal/platform/` 包含跨平台路径解析（macOS/Windows/Linux + 环境变量覆盖）
- [ ] JSON 持久化：`config.json`、`registry.json`、`tags.json`，原子写
- [ ] 每个模块有 table-driven tests（序列化/反序列化、边界情况）
- [ ] 数据模型跟 Kitter 的领域概念对齐（字段可不同，概念一致）

## Blocked by

None — can start immediately.
