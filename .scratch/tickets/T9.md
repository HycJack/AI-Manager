## Parent

- #1 — AI-Manager: Kitter 功能移植

## What to build

前端组织/标签 flow：分组管理（创建/编辑/删除/移动技能）+ 标签两层树（创建/重命名/删除/分配）+ 拖拽分配（@dnd-kit）。

## Acceptance criteria

- [ ] 分组管理：创建、编辑（重命名）、删除（可选移动技能到另一个组）
- [ ] 标签两层树：创建（parent + children）、重命名、删除、展开/折叠
- [ ] 标签分配：技能 ↔ 标签的分配/取消分配
- [ ] 拖拽分配：技能拖到标签上自动分配（@dnd-kit）
- [ ] 标签对话框（dialog）：创建/编辑标签
- [ ] 调用 `ListTags`、`AddTag`、`RenameTag`、`DeleteTag`、`AssignTag`、`ListGroups`、`AddGroup`、`DeleteGroup`、`MoveSkillToGroup` API
- [ ] 标签/分组数据持久化（tags.json）

## Blocked by

- #4 — T3: 前端 Skills 页壳
- #8 — T8: 前端 Projects 页
