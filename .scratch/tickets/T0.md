## Parent

- #1 — AI-Manager: Kitter 功能移植

## What to build

补齐前端缺失的 shadcn 组件（dialog、popover、dropdown-menu、tooltip、input、checkbox、badge、card、select、alert），并将 Wails3 bindings 包名从 `skeleton` 改为 `ai-manager`（Go module path + 前端所有 import 路径）。这是后续所有 UI 工作的前置。

## Acceptance criteria

- [ ] 10 个 shadcn 组件可 import 使用（`@/components/ui/dialog` 等）
- [ ] `main.go` 的 module path 从 `skeleton/internal/app` 改为 `ai-manager/internal/app`
- [ ] 前端所有 `@bindings/skeleton/...` import 改为 `@bindings/ai-manager/...`
- [ ] `wails generate bindings` 重新生成 `frontend/bindings/` 后无报错
- [ ] 现有 SettingsPage 和 HomePage 仍正常编译运行

## Blocked by

None — can start immediately.
