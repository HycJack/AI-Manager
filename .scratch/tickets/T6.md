## Parent

- #1 — AI-Manager: Kitter 功能移植

## What to build

Go 后端项目 + 安装模块。实现 `internal/project/`、`internal/agents/`、`internal/effective/` 模块，包含 InstallTarget 10 变体、symlink 创建/删除、agent 扫描、有效技能计算（跨 agent 扫描 + token 估算）。通过 Wails3 bindings 暴露 `InstallSkills`、`UninstallSkill`、`ListInstallations`、`ListProjects`、`AddProject`、`RemoveProject`、`BrowseProject`、`GetEffectiveSkills`。

## Acceptance criteria

- [ ] `internal/project/`：install、uninstall、scan_project
- [ ] `internal/agents/`：AgentKind 10 变体、InstallTarget 10 变体、target_directory 映射
- [ ] `internal/effective/`：GetEffectiveSkills（跨 agent 扫描 + token 估算）
- [ ] symlink 创建/删除（跨平台：macOS/Linux symlink，Windows junction）
- [ ] `InstallSkills(names, project, targets[], global)` 支持多 target 安装
- [ ] `UninstallSkill(project, name, target)` 卸载
- [ ] `BrowseProject()` 文件夹浏览（Wails3 OpenFolder）
- [ ] `GetEffectiveSkills(project)` 返回每个 agent 的技能 + token 估算
- [ ] table-driven tests（安装/卸载/扫描/有效技能）

## Blocked by

- #2 — T1: Go 后端数据模型 + 持久化
