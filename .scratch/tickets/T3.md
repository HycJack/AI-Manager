## Parent

- #1 — AI-Manager: Kitter 功能移植

## What to build

前端 Skills 页壳：替换现有 `HomePage.tsx`，实现技能列表（虚拟化、搜索过滤、分组折叠）+ 详情面板（多标签：描述 / 安装 / 文件）。使用 `@tanstack/react-virtual` 做虚拟列表，`react-resizable-panels` 做列表+详情可调整大小。

## Acceptance criteria

- [ ] `SkillsPage.tsx` 替换 `HomePage.tsx`，侧边栏导航更新
- [ ] 技能列表：虚拟化渲染、搜索过滤（按名称）、分组折叠、选中高亮
- [ ] 详情面板：多标签（描述 / 安装 / 文件），描述可展开/折叠
- [ ] 列表 + 详情可调整大小（react-resizable-panels）
- [ ] 多选（click、Shift+click 范围、Cmd/Ctrl+click 切换）
- [ ] 调用 `ListSkills`、`GetSkill` API 显示真实数据
- [ ] 空状态（无技能时显示引导）

## Blocked by

- #3 — T2: Go 后端技能库 API
